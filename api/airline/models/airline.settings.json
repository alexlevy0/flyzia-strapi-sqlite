{
  "kind": "collectionType",
  "collectionName": "airlines",
  "info": {
    "name": "airline"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "name": {
      "type": "string",
      "unique": true,
      "required": true
    },
    "description": {
      "type": "richtext"
    },
    "categories": {
      "via": "airlines",
      "collection": "category"
    }
  }
}
